## Commands

- Build the library: `swift build` (from repo root).
- No `swift test` target exists — there are no test files/dir in this repo.
- Run the example app: `cd TagTextViewExample && pod install`, then open
  `TagTextViewExample.xcworkspace` in Xcode (NOT the `.xcodeproj` — CocoaPods requires the workspace).
- No lint/format tool is configured (no SwiftLint/SwiftFormat config found in the repo).
